#include "main.h"


/**
 * _islower - checking for upper and lower case
 * @c: is a letter
 * Return: 1 for lowercase 0 for upper
 */

int _islower(int c)
{
	if (c >= 97 && c <= 122)
	{

	return (1);
	}
	return (0);
}
