#include "main.h"

/**
 * _strlen_recursion - a function that returns the length of a
 * string
 * @s: character input
 * Return: Integer.
 */

int _strlen_recursion(char *s)
{
	int p;

	p = strlen(s);

	return (p);
}
