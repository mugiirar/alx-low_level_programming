#include "main.h"
#include <stdio.h>
#include <stdlib.h>
#include <ctype.h>
#include <string.h>

/**
 * _strlen - returns the length of a string
 * @s: char array
 * Return: Always 0.
 */

int _strlen(char *s)
{
	return (strlen(s));
}
