#include "main.h"

/**
 * main - Determines whether a number is positive or negative
 * @i: a given input
 * Return: Always 0.
 */
int main(void)
{
	int i;

	i = 0;
	positive_or_negative(i);
	return (0);
}
